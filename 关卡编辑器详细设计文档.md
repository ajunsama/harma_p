# 关卡编辑器详细设计文档

---

## 一、概述

### 1.1 目标

重构现有的关卡编辑器，从"波次批量生成"模式升级为"**自由摆放 + 分组触发器**"模式：

- 支持敌人、道具、障碍物三种元素在 2D 画布上的精确摆放。
- 集成过场动画系统（按 `storyId` 引用，不修改剧情系统本身）。
- 引入独立的关卡变量系统，用于控制元素出现条件、关卡结束条件、剧情触发条件。
- 提供 Play Mode 运行时验证与 `.unity` 场景导出功能。
- 提供 JSON 导入/导出能力，方便策划使用外部工具批量修改关卡数据。

### 1.2 与旧系统的关系

本次重构为**彻底重构**，旧系统完全移除：

| 旧组件 | 处理方式 | 说明 |
|---|---|---|
| 旧 `LevelData.cs` | 删除 | 替换为新的数据结构 |
| 旧 `LevelEditorWindow.cs` | 删除 | 替换为新编辑器窗口 |
| `BattleWaveManager.cs` | 删除 | 其职责由 `LevelSceneBuilder` + `ElementGroup` 替代 |
| `LevelProgressManager.cs` | 删除 | 其职责（相机跟随、关卡结束判定、战斗锁屏）由 `LevelSceneBuilder` 直接接管 |
| `BattleAreaVisualizer.cs` | 删除 | 新系统通过 `ElementGroup.mustClearToProceed` + `LevelSceneBuilder` 实现战斗区域逻辑 |
| `StoryManager` / `StoryTrigger` / `StoryUI` | 保留 | 剧情系统本身不变，新系统仅负责生成 `StoryTrigger` 并设置 `storyId` 与变量回调 |
| `PlayerMovement` / `PlayerHP` / 敌人 AI | 保留 | 游戏逻辑不变；敌人 AI 需具备“未指定 player 时自动寻找 Player”的能力 |
| 旧 `Assets/LevelData/*.asset` | 删除 | 不提供迁移工具 |

新架构下，**`LevelSceneBuilder` 是运行时唯一核心**，负责从 `LevelData` 构建完整场景并驱动关卡流程。

### 1.3 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                  LevelEditorWindow（新）                      │
│  ┌──────────┬──────────────────────┬─────────────────────┐  │
│  │ 元素面板  │     2D 编辑画布       │      属性检查器      │  │
│  │(分类预制体│   (拖拽摆放元素)       │   (编辑选中对象)    │  │
│  │ 拖拽放置) │                      │                     │  │
│  └──────────┴──────────────────────┴─────────────────────┘  │
│                              │ 读写                           │
│                              ▼                                │
│                   LevelData（ScriptableObject）                │
│                         │  JSON 导入/导出                      │
│                         ▼                                     │
│   ┌─────────────────────┼─────────────────────┐              │
│   ▼                     ▼                     ▼              │
│ Play Mode 验证      导出 .unity 场景        StorySystem      │
│ （从模板场景生成）   （持久化场景资产）      （storyId 引用）  │
└─────────────────────────────────────────────────────────────┘
```

---

## 二、数据结构设计

### 2.1 `LevelData.cs` — 核心数据结构（ScriptableObject）

```csharp
[CreateAssetMenu(fileName = "NewLevel", menuName = "Game/Level Data", order = 1)]
public class LevelData : ScriptableObject
{
    // ========== 基本信息 ==========
    public string levelName = "New Level";
    [Range(1, 5)] public int difficulty = 1;
    public float levelLength = 50f;

    // ========== 玩家预制体 ==========
    [Tooltip("玩家预制体，默认可使用 Bridge_PV 场景中的 Kaho 对象生成的 Prefab")]
    public GameObject playerPrefab;

    // ========== 背景图 ==========
    public BackgroundSettings backgroundSettings = new BackgroundSettings();

    // ========== 玩家出生点 ==========
    public Vector2 playerSpawnPosition = new Vector2(-8f, -3.5f);
    public bool playerFaceRight = true;

    // ========== 关卡结束条件 ==========
    public float levelEndPositionX = 45f;
    public List<LevelVariableCondition> endConditions = new List<LevelVariableCondition>();

    // ========== 关卡变量 ==========
    public List<LevelVariableDefinition> variables = new List<LevelVariableDefinition>();

    // ========== 元素列表 ==========
    public List<LevelElement> elements = new List<LevelElement>();

    // ========== 过场动画触发器 ==========
    public List<StoryTriggerPoint> storyTriggers = new List<StoryTriggerPoint>();

    // ========== 元素组 ==========
    public List<ElementGroup> groups = new List<ElementGroup>();

    // ========== 预制体库（编辑器辅助）==========
    public List<ElementPrefabEntry> enemyPrefabLibrary = new List<ElementPrefabEntry>();
    public List<ElementPrefabEntry> itemPrefabLibrary = new List<ElementPrefabEntry>();
    public List<ElementPrefabEntry> obstaclePrefabLibrary = new List<ElementPrefabEntry>();

    // ========== 校验 ==========
    public bool Validate(out LevelValidationResult result);

    // ========== JSON 导入/导出 ==========
    public string ExportToJson();
    public void ImportFromJson(string json);
}
```

### 2.2 背景设置 `BackgroundSettings`

当前先实现单张无限循环背景，但预留多层视差扩展接口。

```csharp
public enum BackgroundMode
{
    SingleInfiniteScroll,   // 当前主要实现
    ParallaxLayers          // 预留扩展
}

[Serializable]
public class ParallaxLayerData
{
    public Sprite sprite;
    public float parallaxFactor = 0.5f;
    public int sortingOrder;
    public bool infiniteHorizontal = true;
}

[Serializable]
public class BackgroundSettings
{
    public BackgroundMode mode = BackgroundMode.SingleInfiniteScroll;

    // 单张无限循环背景
    public Sprite singleBackground;
    public float singleParallaxFactor = 0.5f;
    public int singleSortingOrder = -10;

    // 多层视差（预留）
    public List<ParallaxLayerData> parallaxLayers = new List<ParallaxLayerData>();
}
```

### 2.3 元素类型定义

```csharp
/// <summary>元素分类</summary>
public enum ElementType
{
    Enemy,      // 敌人
    Item,       // 道具
    Obstacle    // 障碍物
}

/// <summary>单个摆放元素</summary>
[Serializable]
public class LevelElement
{
    public string elementId;                          // 唯一 GUID
    public string displayName;                        // 编辑器显示名称
    public ElementType elementType;                   // 类型
    public GameObject prefab;                         // 预制体引用

    // 位置与朝向
    public Vector2 position;
    public bool faceRight = true;

    // 出现时机（相对于所在组被触发后的延迟，无组时相对关卡开始）
    public float appearDelay;

    // 所属组（空字符串 = 不属于任何组，随关卡开始直接生成）
    public string groupId;

    // 出现条件
    public List<LevelVariableCondition> appearConditions = new List<LevelVariableCondition>();

    // 自定义参数（覆盖预制体组件上的字段）
    public List<ElementCustomParameter> customParameters = new List<ElementCustomParameter>();
}

/// <summary>自定义参数</summary>
[Serializable]
public class ElementCustomParameter
{
    public string componentTypeName;    // 组件全名，如 "Enemy"
    public string fieldName;            // 字段名，如 "maxHp"
    public string valueTypeName;        // 值类型全名，如 "System.Int32"
    public string serializedValue;      // JSON 序列化的值
}

/// <summary>预制体库条目</summary>
[Serializable]
public class ElementPrefabEntry
{
    public string displayName;
    public GameObject prefab;
    public Sprite icon;                 // 可选，编辑器图标
}
```

**自定义参数支持类型（白名单）：**

| 类型 | 说明 |
|---|---|
| `int` / `float` / `bool` / `string` | 基础类型 |
| `Enum` | 枚举类型，存储枚举名 |
| `Vector2` / `Vector3` | Unity 向量 |
| `Color` | 颜色 |
| `AnimationCurve` | 动画曲线（JSON 序列化较复杂，首期可暂不实现） |

**不支持类型：**`UnityEngine.Object` 引用（如 Sprite、AudioClip、Material 等）。如需引用类型，建议在 Prefab 上直接配置，不通过关卡编辑器覆盖。

### 2.4 关卡变量系统

```csharp
/// <summary>变量类型</summary>
public enum LevelVariableType { Bool, Int, Float, String }

/// <summary>变量定义</summary>
[Serializable]
public class LevelVariableDefinition
{
    public string variableName;
    public LevelVariableType type;
    public string defaultValue;
    public string description;
}

/// <summary>变量条件</summary>
[Serializable]
public class LevelVariableCondition
{
    public enum CompareMode
    {
        Equals, NotEquals, Greater, GreaterOrEqual,
        Less, LessOrEqual, Contains, IsTrue, IsFalse
    }
    public string variableName;
    public CompareMode mode;
    public string compareValue;
}

/// <summary>变量设置动作</summary>
[Serializable]
public class VariableSetAction
{
    public string variableName;
    [Tooltip("根据变量定义的类型进行解析")]
    public string stringValue;
}
```

### 2.5 过场动画触发器

```csharp
[Serializable]
public class StoryTriggerPoint
{
    public float positionX;
    public string storyId;                               // 对应 StoryData JSON 中的 storyId
    public bool triggerOnce = true;
    public bool triggerFromLeft = true;                  // true=玩家从左向右越过时触发
    public List<LevelVariableCondition> triggerConditions = new List<LevelVariableCondition>();

    // 剧情开始/结束时设置的关卡变量（与 StoryManager 的 flag 独立）
    public List<VariableSetAction> onStoryStartSetVariables = new List<VariableSetAction>();
    public List<VariableSetAction> onStoryCompleteSetVariables = new List<VariableSetAction>();
}
```

### 2.6 元素组

```csharp
[Serializable]
public class ElementGroup
{
    public string groupId;               // 唯一 GUID
    public string groupName;             // 编辑器显示名称
    public float triggerPositionX;       // 玩家到达此 X 触发该组
    public bool mustClearToProceed;      // 是否必须清完敌人才能继续前进
    public List<LevelVariableCondition> triggerConditions = new List<LevelVariableCondition>();
}
```

---

## 三、道具和障碍物基础类型（占位符）

### 3.1 道具类型 `ItemType`

```csharp
public enum ItemType
{
    HealthRecovery,     // 回血
    Invincible,         // 临时无敌
    SpeedBoost,         // 加速
    ScoreBonus,         // 加分
    Custom              // 自定义（通过自定义参数扩展）
}
```

### 3.2 `ItemPickup.cs` — 道具基类（Placeholder）

```csharp
public class ItemPickup : MonoBehaviour
{
    public ItemType itemType = ItemType.HealthRecovery;
    public float effectValue = 1f;          // 效果数值
    public float effectDuration = 0f;       // 效果持续时间（0 = 瞬时）

    // 后续根据策划设计补充实际效果逻辑
    void OnTriggerEnter2D(Collider2D other)
    {
        if (other.CompareTag("Player"))
        {
            ApplyEffect(other.gameObject);
            Destroy(gameObject);
        }
    }

    void ApplyEffect(GameObject player) { /* 后续实现 */ }
}
```

### 3.3 障碍物类型 `ObstacleType`

```csharp
public enum ObstacleType
{
    StaticBlock,        // 静态阻挡（不可通过不可破坏）
    Destructible,       // 可破坏（有 HP）
    MovingPlatform,     // 移动平台
    Custom              // 自定义
}
```

### 3.4 `Obstacle.cs` — 障碍物基类（Placeholder）

```csharp
public class Obstacle : MonoBehaviour
{
    public ObstacleType obstacleType = ObstacleType.StaticBlock;
    public float hp = 1f;                   // 可破坏类型的生命值
    public bool blocksPlayer = true;        // 是否阻挡玩家
    public bool blocksEnemy = false;        // 是否阻挡敌人

    // 后续根据策划设计补充实际逻辑
}
```

---

## 四、编辑器窗口设计

### 4.1 入口

- Unity 菜单：`Tools → 关卡编辑器`
- 快捷键：`Ctrl+Shift+L`
- 直接替换旧版编辑器的菜单入口

### 4.2 窗口布局

```
┌──────────────────────────────────────────────────────────────┐
│  工具栏: [新建] [打开] [保存] [Play测试] [导出Scene] [清空] [校验] │
│  关卡名: xxx    元素: xx    组: xx    变量: xx                  │
├──────────┬───────────────────────────────┬────────────────────┤
│ 元素面板  │        2D 编辑画布             │  属性面板           │
│ ┌──────┐ │  ╔══ 过场 ╗                  │ ┌────────────────┐ │
│ │敌人   │ │  ║level1_ ║      ▲ ▲       │ │ 类型: 敌人      │ │
│ │MuscleP│ │  ║opening║      ▲ ▲       │ │ 预制体:MuscleP │ │
│ │FatP   │ │  ╚════════╝      ▲         │ │ 位置X: 15.0    │ │
│ │PunkP  │ │  起点           ▲          │ │ 位置Y: -3.5    │ │
│ │Enemy  │ │  [P]──────◆◆──◆◆────██终点 │ │ 朝向: 右       │ │
│ ├──────┤ │         ║过场╗              │ │ 延迟: 0.5s     │ │
│ │道具   │ │         ║boss║   ★ ★       │ │ 组: 第1组      │ │
│ │回血药  │ │         ╚════╝             │ │ 条件: 无       │ │
│ │无敌星  │ │            ■ ■            │ │               │ │
│ ├──────┤ │   ═══ 地板 ═══════════════ │ │ [删除元素]     │ │
│ │障碍物 │ │  0    10    20    30    40  │ └────────────────┘ │
│ │木箱   │ │                               │                   │
│ │石墙   │ │                               │                   │
│ └──────┘ │                               │                   │
├──────────┴───────────────────────────────┴────────────────────┤
│  底部面板: [列表视图] [时间轴视图]                               │
│  #  │ 名称     │ 类型  │ 位置X │ 位置Y │ 延迟  │ 组        │
│  1  │ 回血药   │ 道具  │ 12.0  │ -2.5  │ 0.2s  │ 第1组    │
│  2  │ MuscleP  │ 敌人  │ 15.0  │ -3.5  │ 0.5s  │ 第1组    │
│  3  │ 木箱     │ 障碍物│ 32.0  │ -3.5  │ 0s    │ 无       │
└──────────────────────────────────────────────────────────────┘
```

### 4.3 图例

| 图标 | 含义 |
|------|------|
| `[P]` 绿色圆形+箭头 | 玩家出生点 |
| `██` 黑白格子旗标 | 关卡终点 |
| `▲` 红色三角 | 敌人（尖头表示朝向） |
| `★` 黄色星形 | 道具 |
| `■` 灰色方块 | 障碍物 |
| `║` 青色竖线+storyId | 过场动画触发器 |
| `▎` 橙色虚线 | 元素组触发线 |
| `══` 绿色线 | 地面/地板参考线 |

### 4.4 各面板功能

#### 工具栏

| 按钮 | 功能 | 快捷键 |
|------|------|--------|
| 新建 | 创建 LevelData.asset，弹出保存面板 | — |
| 打开 | 打开已有 LevelData.asset | — |
| 保存 | 保存当前关卡数据 | Ctrl+S |
| Play测试 | 从模板场景生成全新临时场景并进入 Play Mode | — |
| 导出Scene | 导出为 .unity 场景文件 | — |
| 清空 | 清除所有元素（二次确认） | — |
| 校验 | 验证数据完整性，输出错误/警告 | — |
| 导出JSON | 将 LevelData 导出为 JSON 文本 | — |
| 导入JSON | 从 JSON 文本覆盖当前 LevelData | — |

#### 元素面板（左侧）

- 按**敌人 / 道具 / 障碍物**分类折叠
- 每项显示预制体名称
- **单击**选中一个条目 → 再单击画布放置
- **拖拽**条目到画布 → 直接放置
- 条目来源于当前 `LevelData` 中配置的预制体库
- 提供"扫描预制体"按钮，自动按组件类型填充三个库

#### 2D 编辑画布（中央）

**导航**：
- 横向可滚动（关卡是横版长卷）
- 鼠标滚轮缩放（3x ~ 15x）
- 中键拖拽平移视口

**元素交互**：
- 点击元素 → 选中（高亮边框），右侧属性面板显示详情
- 拖拽元素 → 移动位置（吸附网格可选）
- 右键菜单 → 删除、复制、移至顶层/底层
- Delete → 删除选中元素
- 框选 → 选中多个元素

**视觉元素**：
- 地面参考线
- X 轴刻度（每 5 单位标记，每 10 单位数字）
- 组触发线（橙色虚线）
- 关卡背景图缩略预览（叠加在底层）

#### 属性面板（右侧）

上下文相关，选中不同类型显示不同内容：

| 选中类型 | 显示内容 |
|----------|----------|
| 关卡（空白处） | 关卡名称、长度、难度、玩家预制体、背景图(Sprite)、玩家起点、终点位置 |
| 元素 | 类型、预制体(ObjectField)、位置XY、朝向(Toggle)、延迟(Float)、所属组(Dropdown)、出现条件(列表)、自定义参数(列表) |
| 过场触发器 | 位置X、storyId(字符串/Dropdown)、触发方向、是否只触发一次、触发条件、剧情开始时变量设置、剧情结束时变量设置 |
| 组 | 组名、触发位置X、是否必须清怪、触发条件 |

#### 底部面板

- **列表视图**（默认）：表格形式显示所有元素，可排序
- **时间轴视图**：按组显示元素的时序分布

---

## 五、运行时系统设计

### 5.1 `LevelSceneBuilder.cs` — 关卡场景构建器

**定位**：`BattleWaveManager` 与 `LevelProgressManager` 的替代者，是运行时唯一关卡驱动器。

**挂载位置**：场景根对象（Play 测试时自动创建，导出 Scene 时写入）。

```csharp
public class LevelSceneBuilder : MonoBehaviour
{
    [Header("关卡数据")]
    public LevelData levelData;

    [Header("场景引用（运行时自动创建/查找）")]
    public Camera mainCamera;
    public Transform playerTransform;

    // 内部组件
    private LevelVariableManager _variableManager;
    private LevelCameraController _cameraController;

    // 生成对象追踪
    private List<GameObject> _spawnedObjects = new List<GameObject>();
    private Dictionary<string, List<GameObject>> _groupEnemyInstances = new Dictionary<string, List<GameObject>>();
    private List<string> _triggeredGroups = new List<string>();
    private List<StoryTriggerPoint> _finishedStoryTriggers = new List<StoryTriggerPoint>();

    // 元素按组缓存
    private Dictionary<string, List<LevelElement>> _elementsByGroup;
    private List<LevelElement> _ungroupedElements;

    void Start()
    {
        BuildSceneInfrastructure();
        InitializeVariables();
        BuildLevel();
    }

    void Update()
    {
        if (playerTransform == null) return;

        CheckElementAppearConditions();
        CheckGroupTriggers();
        CheckStoryTriggers();
        CheckLevelEnd();
        UpdateCameraLock();
    }

    // ========== 场景构建 ==========
    void BuildSceneInfrastructure();   // 创建/查找地面、相机、StoryManager、Canvas 等
    void CreateBackground();           // 根据 BackgroundSettings 创建背景
    void CreatePlayer();               // 实例化 playerPrefab 于 playerSpawnPosition
    void CreateFloor();                // 从模板场景复制 Floor 对象或创建默认地面
    void SetupCamera();                // 设置主相机并挂载 LevelCameraController
    void SetupStorySystem();           // 确保场景中存在 StoryManager、Canvas/StoryUI、EventSystem

    // ========== 关卡内容 ==========
    void BuildLevel();
    void InitializeGroups();
    void SpawnUngroupedElements();     // 生成无组元素
    void CheckGroupTriggers();         // 检测玩家位置触发组
    void TriggerGroup(ElementGroup group);
    void SpawnGroup(ElementGroup group);
    GameObject SpawnElement(LevelElement el);
    void ApplyCustomParameters(GameObject go, LevelElement el);

    // ========== 剧情触发 ==========
    void CheckStoryTriggers();
    void SetupStoryTrigger(StoryTriggerPoint point);
    void OnStoryStart(StoryTriggerPoint point);
    void OnStoryComplete(StoryTriggerPoint point);

    // ========== 结束与清理 ==========
    void CheckLevelEnd();
    void UpdateCameraLock();           // mustClearToProceed 组的相机/玩家边界锁定
    void OnGroupCleared(ElementGroup group);
    void CompleteLevel();
    void ClearAll();
}
```

**关键流程说明**：

1. **BuildSceneInfrastructure**
   - 创建 `LevelVariableManager` 并挂载到自身。
   - 从关卡数据或模板场景复制 `Floor`（带 `BoxCollider2D`）到当前场景，供 `LevelManager` 计算边界。
   - 创建/查找 `Main Camera`，挂载 `LevelCameraController`。
   - 确保 `LevelManager` 存在。
   - 确保 `StoryManager`、`Canvas/StoryUI`、`EventSystem` 存在（Play 测试时从模板场景复制；导出 Scene 时直接保留在场景中）。

2. **CreatePlayer**
   - 使用 `levelData.playerPrefab`（默认指向从 Bridge_PV 的 Kaho 制作的 Prefab）。
   - 实例化到 `playerSpawnPosition`，按 `playerFaceRight` 设置 `localScale.x`。
   - 设置 tag 为 `Player`。

3. **CreateBackground**
   - `BackgroundMode.SingleInfiniteScroll`：创建 SpriteRenderer + `InfiniteScrollBackground`，设置 `Sprite`、`parallaxFactor`、`sortingOrder`。
   - `BackgroundMode.ParallaxLayers`（预留）：创建 `ParallaxBackground` 并按层配置。

4. **BuildLevel**
   - 创建背景。
   - 创建玩家。
   - 初始化变量。
   - 缓存元素：`ungroupedElements` 与 `elementsByGroup[groupId]`。
   - 生成无组元素（满足 `appearConditions` 的立即生成，不满足的进入待观察列表）。
   - 注册组触发器（按 `triggerPositionX` 排序）。
   - 创建过场触发器 GameObject（挂载 `StoryTrigger`，类型 `Position`，并绑定开始/结束变量设置回调）。

5. **SpawnElement**
   - `Instantiate(prefab, position, rotation)`
   - 设置朝向：`transform.localScale.x = faceRight ? 1 : -1`
   - 调用 `ApplyCustomParameters` 覆盖组件字段
   - 若元素为敌人且属于某组，加入 `_groupEnemyInstances[groupId]`
   - 加入 `_spawnedObjects`

6. **ApplyCustomParameters**
   - 反射获取 `componentTypeName` 对应组件
   - 获取 `fieldName` 字段（含 `[SerializeField]` 的私有字段与 public 字段）
   - 按 `valueTypeName` 将 `serializedValue` 反序列化后写入

7. **CheckGroupTriggers**
   - 遍历未触发的组，若玩家 X 越过 `triggerPositionX` 且满足 `triggerConditions`，调用 `TriggerGroup`

8. **TriggerGroup**
   - 若 `mustClearToProceed`，锁定相机/玩家边界
   - 按 `appearDelay` 依次生成组内元素（可用协程或时间轮）

9. **CheckStoryTriggers**
   - 与 `StoryTrigger` 自带的位置检测复用：由 `LevelSceneBuilder` 生成的 `StoryTrigger` 对象会在 `Update` 中自行检测。
   - `StoryTrigger` 触发时通过 `OnBeforeStory` / `OnAfterStory` 回调通知 `LevelSceneBuilder` 设置变量。

10. **CheckLevelEnd**
    - 玩家 X >= `levelEndPositionX` 且满足 `endConditions` 时调用 `CompleteLevel`

11. **UpdateCameraLock**
    - 当存在激活的 `mustClearToProceed` 组且组内仍有敌人时，锁定相机位置与玩家移动边界。
    - 组内敌人全部死亡后解锁。

### 5.2 `LevelVariableManager.cs` — 运行时变量管理

```csharp
public class LevelVariableManager : MonoBehaviour
{
    private Dictionary<string, object> _variables = new Dictionary<string, object>();
    private Dictionary<string, List<Action>> _listeners = new Dictionary<string, List<Action>>();

    void Initialize(List<LevelVariableDefinition> definitions);
    void SetVariable(string name, object value);   // 设置变量 + 触发回调
    T GetVariable<T>(string name);
    bool TryGetVariable(string name, out object value);
    bool CheckCondition(LevelVariableCondition c);
    bool CheckAllConditions(List<LevelVariableCondition> conditions);
    void OnVariableChanged(string name, Action callback);
    void RemoveListener(string name, Action callback);

    // 执行 VariableSetAction
    void ApplySetAction(VariableSetAction action);
}
```

### 5.3 `LevelCameraController.cs` — 相机控制（新增）

替代原 `LevelProgressManager` 的相机逻辑。

```csharp
public class LevelCameraController : MonoBehaviour
{
    public Transform target;                  // 玩家
    public float smoothSpeed = 5f;
    public float fixedY = 0f;
    public float zOffset = -10f;
    public bool lockPosition;                 // 战斗锁屏
    public float lockX;                       // 锁屏时的 X 坐标
    public float minX;                        // 相机最小 X
    public float maxX;                        // 相机最大 X

    void LateUpdate()
    {
        float targetX = lockPosition ? lockX : target.position.x;
        targetX = Mathf.Clamp(targetX, minX, maxX);
        Vector3 desired = new Vector3(targetX, fixedY, zOffset);
        transform.position = Vector3.Lerp(transform.position, desired, Time.deltaTime * smoothSpeed);
    }
}
```

### 5.4 与剧情系统集成

- 编辑器配置 `StoryTriggerPoint` → 运行时 `LevelSceneBuilder` 在触发位置创建隐形 GameObject。
- 挂载 `StoryTrigger` 组件，设置：
  - `triggerType = Position`
  - `storyId = StoryTriggerPoint.storyId`
  - `triggerOnce = StoryTriggerPoint.triggerOnce`
  - `useTransformPosition = false`
  - `triggerPositionX = StoryTriggerPoint.positionX`
  - `triggerFromLeft = StoryTriggerPoint.triggerFromLeft`
- 将 `StoryTriggerPoint.onStoryStartSetVariables` 绑定到 `StoryTrigger.OnBeforeStory`。
- 将 `StoryTriggerPoint.onStoryCompleteSetVariables` 绑定到 `StoryTrigger.OnAfterStory`。
- 回调内部调用 `LevelVariableManager.ApplySetAction`。
- 完全复用已有的 `StoryManager.PlayStory()` 流程。

### 5.5 Play 测试流程

1. 用户点击「Play测试」。
2. 编辑器保存当前 `LevelData`（提示用户）。
3. 编辑器创建新 Scene（`NewSceneSetup.EmptyScene`）。
4. 从「模板场景」复制必要基础设施到当前场景：
   - `Floor`（地面）
   - `Main Camera`
   - `Canvas`（含 `StoryRoot` / `StoryUI`）
   - `EventSystem`
   - `StoryManager`（含 `storyJsonFile`、`templateLibrary` 引用）
5. 在当前场景创建 `LevelSceneBuilder` 根对象，并挂载 `LevelVariableManager`。
6. 将当前编辑的 `LevelData` 赋值给 `LevelSceneBuilder`。
7. 调用 `EditorApplication.EnterPlaymode()`。
8. `LevelSceneBuilder.Start()` → `BuildSceneInfrastructure()` → `BuildLevel()`。
9. 退出 Play Mode 时，`LevelSceneBuilder.OnDestroy()` 清理临时对象；场景不保存。

**模板场景**：建议基于 `Assets/Scenes/Bridge_PV.unity` 制作一个专用的 `Assets/Scenes/LevelEditorTestTemplate.unity`，仅保留：

- `Floor`
- `Main Camera`
- `Canvas`（含 `StoryRoot`）
- `EventSystem`
- `StoryManager`（配置好 `storyJsonFile`、`templateLibrary`）
- `LevelManager`

去除 `Kaho`、`BattleWaveManager`、`LevelProgressManager`、所有敌人、旧 StoryTrigger 等会被动态生成的对象。

### 5.6 Scene 导出流程

1. 用户点击「导出Scene」→ 选择 .unity 保存路径。
2. 创建新 Scene（`NewSceneSetup.EmptyScene`）。
3. 从模板场景复制基础设施（与 Play 测试一致）。
4. 写入 GameObjects：
   - 背景（SpriteRenderer + InfiniteScrollBackground）
   - 玩家（实例化 Prefab 在起点）
   - 所有元素（实例化 Prefab 并设置 Transform）
   - 过场触发器（挂载 StoryTrigger 的隐形对象）
   - 管理对象（`LevelSceneBuilder` + `LevelVariableManager`，引用 `LevelData`）
5. `EditorSceneManager.SaveScene()`。

导出后的场景可直接运行，不再依赖编辑器。

---

## 六、编辑器 UI 技术方案

### 6.1 技术选型

- 使用 **IMGUI**（`EditorWindow + GUILayout + Handles`），与项目现有编辑器风格一致。
- 2D 画布使用 `GUI.BeginClip + Handles API` 绘制。
- 不使用 UI Toolkit。

### 6.2 画布实现细节

- 视口变换：`offsetX = rect.x + 50 - scrollOffset.x`，`worldToScreenX = offsetX + worldX * scale`
- 缩放：`scale = Mathf.Clamp(scale - event.delta.y * 0.3f, 3f, 15f)`
- 拖拽选中元素：距离检测，在屏幕空间判断鼠标与元素图标中心距离
- Gizmo 绘制：利用 `Handles.color` 设置颜色，`Handles.DrawSolidDisc`、`DrawTriangle` 绘制图标

### 6.3 撤销/重做

所有对 `LevelData` 的修改必须包裹 `Undo.RecordObject`：

```csharp
Undo.RecordObject(levelData, "Move Element");
levelElement.position = newPosition;
EditorUtility.SetDirty(levelData);
```

支持的操作：
- 添加/删除元素
- 移动元素位置
- 修改元素属性
- 添加/删除组
- 添加/删除变量
- 添加/删除过场触发器

### 6.4 自定义参数编辑

- 选中元素时，反射读取其 Prefab 所有组件的 public 字段与 `[SerializeField]` 私有字段。
- 为每种类型生成对应 `EditorGUILayout` 控件：
  - `float` → `EditorGUILayout.FloatField`
  - `int` → `EditorGUILayout.IntField`
  - `bool` → `EditorGUILayout.Toggle`
  - `string` → `EditorGUILayout.TextField`
  - `Enum` → `EditorGUILayout.EnumPopup`
  - `Vector2` → `EditorGUILayout.Vector2Field`
  - `Vector3` → `EditorGUILayout.Vector3Field`
  - `Color` → `EditorGUILayout.ColorField`
- 用户修改时更新 `ElementCustomParameter` 列表。
- 未显式覆盖的参数使用 Prefab 默认值。

### 6.5 预制体自动扫描

```csharp
void ScanPrefabs()
{
    // 扫描 Assets/Prefabs 下所有 .prefab
    string[] guids = AssetDatabase.FindAssets("t:Prefab", new[] { "Assets/Prefabs" });
    foreach (var guid in guids)
    {
        var prefab = AssetDatabase.LoadAssetAtPath<GameObject>(AssetDatabase.GUIDToAssetPath(guid));
        // 按组件分类
        if (prefab.GetComponent<Enemy>() != null)      levelData.enemyPrefabLibrary.Add(...);
        if (prefab.GetComponent<ItemPickup>() != null) levelData.itemPrefabLibrary.Add(...);
        if (prefab.GetComponent<Obstacle>() != null)   levelData.obstaclePrefabLibrary.Add(...);
    }
}
```

---

## 七、JSON 导入/导出

### 7.1 导出

```csharp
public string ExportToJson()
{
    // 使用 JsonUtility 序列化，GameObject/Sprite 等引用存储为 GUID
    var dto = LevelDataDto.FromLevelData(this);
    return JsonUtility.ToJson(dto, true);
}
```

**引用处理**：
- `GameObject` / `Sprite` 等 Unity 对象引用存储为 `AssetDatabase.AssetPathToGUID(path)`。
- 导入时通过 `AssetDatabase.GUIDToAssetPath(guid)` 重新解析。

### 7.2 导入

```csharp
public void ImportFromJson(string json)
{
    var dto = JsonUtility.FromJson<LevelDataDto>(json);
    dto.ToLevelData(this);
    EditorUtility.SetDirty(this);
}
```

### 7.3 使用场景

- 策划可在版本控制中对比 JSON 差异。
- 可用外部文本编辑器批量调整坐标、延迟等数值。
- 导入时若 GUID 失效（资源被移动/删除），给出警告并保留空引用。

---

## 八、关卡校验规则

`LevelData.Validate(out LevelValidationResult result)` 检查以下项：

### 错误（Error）

- 关卡名称为空
- 玩家预制体为空
- 玩家出生点 Y 轴超出合理范围（可作为 Warning）
- 终点位置 X 小于等于起点位置 X
- 元素引用的 Prefab 为空
- 过场触发器 `storyId` 为空
- 组触发器位置 X 超出关卡范围（0 ~ levelLength）
- 引用的变量名在变量列表中不存在
- 自定义参数的 `componentTypeName` 或 `fieldName` 为空

### 警告（Warning）

- 关卡没有任何元素
- 元素组内没有元素
- 元素组 `triggerPositionX` 小于玩家起点 X
- 没有配置任何过场动画（不是错误，只是提醒）
- 没有配置背景图
- 引用的 Prefab 在项目中不存在
- 玩家出生点 X 为负数（横版游戏常见，仅提醒）

**注**：出生点 X 不再强制 ≥ 0，因为横版卷轴游戏中玩家可能从屏幕外进入。

---

## 九、文件清单

### 新增/重写文件

| 文件 | 路径 | 说明 |
|------|------|------|
| `LevelData.cs` | `Assets/Scripts/LevelSystem/` | **重写** — 全新数据结构 |
| `LevelElement.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 元素、变量、条件、背景等类型定义 |
| `LevelVariableManager.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 运行时变量管理器 |
| `LevelSceneBuilder.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 运行时场景构建器，替代旧波次/进度管理器 |
| `LevelCameraController.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 相机跟随与战斗锁屏 |
| `LevelEditorWindow.cs` | `Assets/Editor/` | **重写** — 新版编辑器窗口 |
| `ItemPickup.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 道具拾取基类（Placeholder） |
| `Obstacle.cs` | `Assets/Scripts/LevelSystem/` | **新建** — 障碍物基类（Placeholder） |
| `LevelEditorTestTemplate.unity` | `Assets/Scenes/` | **新建** — Play 测试/导出 Scene 的模板场景 |

### 旧文件处理

| 旧文件 | 处理方式 |
|--------|----------|
| 旧 `LevelData.cs` | 删除 |
| 旧 `LevelEditorWindow.cs` | 删除 |
| 旧 `BattleWaveManager.cs` | 删除 |
| 旧 `LevelProgressManager.cs` | 删除 |
| 旧 `BattleAreaVisualizer.cs` | 删除 |
| 旧 `Assets/LevelData/*.asset` | 删除 |

### 可能需微调的现有文件

| 文件 | 调整内容 |
|------|----------|
| 敌人 AI 脚本（`MuscleP_AI_Movement` 等） | 在 `Start` 中若 `player` 为 null，自动 `FindGameObjectWithTag("Player")` |

---

## 十、实现计划

| 阶段 | 内容 | 预计工作量 |
|------|------|-----------|
| **Phase 1** | 数据结构重构 — 新 `LevelData`、`LevelElement`、变量系统、背景设置、`ItemPickup`/`Obstacle` 占位符 | 中 |
| **Phase 2** | 模板场景制作 — 基于 `Bridge_PV` 制作 `LevelEditorTestTemplate.unity` | 小 |
| **Phase 3** | 运行时核心 — `LevelVariableManager`、`LevelCameraController`、`LevelSceneBuilder`（场景基础设施、背景、玩家、元素生成、组触发、剧情触发、关卡结束） | 大 |
| **Phase 4** | 编辑器核心 — 窗口框架、画布绘制、元素拖拽放置、属性面板、预制体扫描、撤销/重做 | 大 |
| **Phase 5** | 高级功能 — 变量编辑面板、元素组、过场触发器集成、自定义参数编辑、关卡校验 | 中 |
| **Phase 6** | Play 测试 & 导出 — Play Mode 临时场景生成、Scene 导出、JSON 导入/导出 | 中 |
| **Phase 7** | 打磨 & 清理 — 错误日志优化、快捷键、删除旧文件、回归测试 | 小 |
