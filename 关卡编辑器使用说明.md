# 关卡编辑器使用说明

本文档介绍新版关卡编辑器的使用方法，包括数据配置、编辑器窗口操作、运行时测试与导出流程。

---

## 1. 系统概述

新版关卡编辑器采用 **数据驱动 + 运行时构建** 的架构：

- **LevelData**：ScriptableObject 资源，保存关卡全部配置（出生点、敌人、道具、障碍物、剧情触发、变量、结束条件等）。
- **LevelEditorWindow**：Unity 编辑器窗口，用于可视化编辑 LevelData。
- **LevelSceneBuilder**：运行时组件，读取 LevelData 并在场景中动态生成玩家、背景、敌人、剧情触发器等。
- **LevelVariableManager**：运行时管理关卡独立变量，支持变量条件判断与赋值。
- **LevelCameraController**：相机跟随与战斗锁屏。

旧版的 `BattleWaveManager`、`LevelProgressManager`、`BattleAreaVisualizer` 已移除，请勿继续使用。

---

## 2. 前置准备

### 2.1 必备资源

在使用编辑器前，请确保项目中存在以下资源：

| 资源 | 说明 |
|------|------|
| 玩家预制体 | 一个带有 `PlayerMovement`、`PlayerHP`、`Rigidbody2D` 等组件的玩家角色 Prefab。 |
| 敌人预制体 | 至少一个敌人 Prefab，需挂载 `Enemy` 组件。 |
| 背景 Sprite | 用于 `SingleInfiniteScroll` 模式的背景图，或用于 `ParallaxLayers` 模式的多层视差 Sprite。 |
| 剧情 JSON | `StoryManager` 引用的 TextAsset，包含可用的 `storyId`。 |

### 2.2 模板场景

项目已提供 `Assets/Scenes/LevelEditorTestTemplate.unity` 作为测试/导出模板，包含：

- `Main Camera`
- `Canvas`（含 Story UI）
- `EventSystem`
- `Floor`
- `LevelManager`
- `StoryManager`
- `StaticBg`

Play Test 和 Export Scene 都会基于该模板生成运行时场景。

---

## 3. 创建关卡数据

### 3.1 新建 LevelData

1. 在 Project 窗口中右键 → `Create → Game → Level Data`。
2. 命名为 `Level_xxx` 并放在 `Assets/LevelData/` 目录下。

### 3.2 基础信息

在 Inspector 中配置：

| 字段 | 说明 |
|------|------|
| `levelName` | 关卡显示名称。 |
| `difficulty` | 难度等级（1~5）。 |
| `levelLength` | 关卡总长度，用于校验元素组触发位置和结束位置。 |
| `playerPrefab` | 玩家预制体。 |
| `playerSpawnPosition` | 玩家出生坐标（X, Y）。 |
| `playerFaceRight` | 玩家是否朝右。 |
| `levelEndPositionX` | 终点 X 坐标，玩家到达即通关。 |

### 3.3 背景设置

`backgroundSettings` 支持两种模式：

- **SingleInfiniteScroll**：单张无限滚动背景。
  - `singleBackground`：背景 Sprite。
  - `singleParallaxFactor`：视差系数（0~1）。
  - `singleSortingOrder`：渲染排序层级。
- **ParallaxLayers**：多层视差背景。
  - 在 `parallaxLayers` 中添加多层，每层配置 Sprite、视差系数、排序层级、是否水平循环。

### 3.4 添加元素

`elements` 列表中的每一项代表一个关卡元素：

| 字段 | 说明 |
|------|------|
| `elementId` | 唯一标识，必填。 |
| `displayName` | 显示名称。 |
| `elementType` | `Enemy` / `Item` / `Obstacle`。 |
| `prefab` | 对应预制体。 |
| `position` | 生成位置。 |
| `faceRight` | 是否朝右。 |
| `appearDelay` | 在组内的出现延迟（秒）。 |
| `groupId` | 所属元素组 ID，空表示独立元素。 |
| `appearConditions` | 变量条件，满足后才生成。 |
| `customParameters` | 自定义参数，生成时通过反射写入组件字段。 |

**注意**：
- 独立元素（`groupId` 为空）会在关卡开始时直接生成。
- 组内元素会在组触发后按 `appearDelay` 顺序生成。

### 3.5 元素组

`groups` 用于将敌人/道具等组织成一波：

| 字段 | 说明 |
|------|------|
| `groupId` | 唯一标识，与元素 `groupId` 对应。 |
| `groupName` | 显示名称。 |
| `triggerPositionX` | 触发位置，玩家 X 坐标超过该值时触发。 |
| `mustClearToProceed` | 是否必须清敌才能继续。 |
| `triggerConditions` | 变量条件，满足后才可触发。 |

当 `mustClearToProceed = true` 时：
- 触发时相机会锁定在玩家当前位置。
- 玩家被限制在相机视野内。
- 组内所有敌人死亡后，相机解锁，玩家可继续前进。

### 3.6 剧情触发器

`storyTriggers` 配置剧情触发：

| 字段 | 说明 |
|------|------|
| `positionX` | 触发位置。 |
| `storyId` | 剧情 ID，必须存在于 `StoryManager` 的剧情 JSON 中。 |
| `triggerOnce` | 是否只触发一次。 |
| `triggerFromLeft` | 是否仅从左向右越过时触发。 |
| `triggerConditions` | 变量条件。 |
| `onStoryStartSetVariables` | 剧情开始时执行的变量赋值。 |
| `onStoryCompleteSetVariables` | 剧情结束时执行的变量赋值。 |

### 3.7 关卡变量

`variables` 定义关卡内使用的变量：

| 类型 | 说明 |
|------|------|
| `Bool` | 布尔值。 |
| `Int` | 整型。 |
| `Float` | 浮点型。 |
| `String` | 字符串。 |

变量可用于：
- 元素出现条件（`appearConditions`）
- 元素组触发条件（`triggerConditions`）
- 剧情触发条件（`triggerConditions`）
- 结束条件（`endConditions`）
- 剧情开始/结束时的赋值操作

### 3.8 结束条件

`endConditions` 为变量条件列表，所有条件满足且玩家 X 坐标超过 `levelEndPositionX` 时关卡完成。

---

## 4. 编辑器窗口

### 4.1 打开窗口

菜单栏：`Window → Level Editor`

### 4.2 工具栏

- **New**：新建 LevelData。
- **Open**：打开已有 LevelData。
- **Save / Save As**：保存关卡数据。
- **Validate**：校验关卡配置，检查缺失预制体、未定义变量、触发位置越界等问题。
- **Play Test**：基于模板场景进入 Play Mode 测试。
- **Export Scene**：基于模板场景生成并保存一个 `.unity` 场景文件。
- **Import JSON / Export JSON**：导入/导出 JSON 格式的关卡数据，便于外部编辑。

### 4.3 元素面板

左侧元素类型页签：

- `Enemy`：敌人
- `Item`：道具
- `Obstacle`：障碍物

在关卡数据资产的 `enemyPrefabLibrary`、`itemPrefabLibrary`、`obstaclePrefabLibrary` 中配置常用预制体，即可在面板中快速选择。

### 4.4 画布操作

中间 2D 画布显示关卡横截面：

- **左键框选 / 点选**：选择元素。
- **中键拖拽 / 按住 Space 拖拽**：平移视图。
- **滚轮**：缩放视图。
- **右键菜单**：删除、复制元素。

### 4.5 属性面板

选中元素或组后，右侧面板显示可编辑属性：

- 位置、朝向、预制体
- 出现延迟、所属组
- 出现条件
- 自定义参数覆盖

### 4.6 列表面板

底部列表视图显示所有元素和组，支持：

- 快速定位
- 排序
- 多选
- 重命名

---

## 5. 运行时构建流程

进入 Play Mode 后，`LevelSceneBuilder` 会按以下顺序构建关卡：

1. **初始化变量**：`LevelVariableManager.Initialize(levelData.variables)`。
2. **基础设施**：查找/创建主相机，附加 `LevelCameraController`。
3. **创建背景**：根据 `backgroundSettings` 生成无限滚动或视差背景。
4. **创建玩家**：在 `playerSpawnPosition` 实例化 `playerPrefab`，标记 `Player` 标签。
5. **生成独立元素**：生成 `groupId` 为空的元素。
6. **注册组内元素**：将组内元素缓存，等待触发。
7. **创建剧情触发器**：在 `storyTriggers` 位置生成 `StoryTrigger`。
8. **事件订阅**：订阅 `Enemy.OnEnemyDied` 用于清敌解锁。

运行时更新：

- 每帧检测玩家是否超过组触发位置。
- 检测玩家是否到达终点。
- 当存在 `mustClearToProceed` 的激活组时，保持相机锁屏并跟踪敌人死亡。

---

## 6. 测试与导出

### 6.1 Play Test

1. 在编辑器窗口中点击 **Play Test**。
2. 工具会自动：
   - 打开 `LevelEditorTestTemplate.unity`
   - 在 `LevelManager` 上附加 `LevelSceneBuilder`
   - 赋值当前 `LevelData`
   - 进入 Play Mode
3. 使用键盘控制玩家移动，测试触发器、敌人、剧情、通关逻辑。
4. 退出 Play Mode 后，模板场景保持干净（运行时对象不会保存）。

### 6.2 Export Scene

1. 点击 **Export Scene**，选择保存路径。
2. 工具会基于模板场景生成一个包含所有运行时对象的静态场景。
3. 保存后可直接作为普通场景使用，无需再依赖 `LevelSceneBuilder`。

### 6.3 JSON 导入/导出

- **Export JSON**：将当前 LevelData 序列化为 JSON，便于版本控制或外部工具编辑。
- **Import JSON**：从 JSON 反序列化并覆盖当前 LevelData。

JSON 中预制体以 `prefabAssetPath`（如 `Assets/Prefabs/PunkP.prefab`）保存，导入时自动解析为 GUID。

---

## 7. 注意事项

1. **玩家预制体必须正确配置**：包含 `Rigidbody2D`、`PlayerMovement`、`PlayerHP`，且能被 `LevelSceneBuilder` 正常实例化。
2. **敌人预制体必须包含 `Enemy` 组件**：否则 `mustClearToProceed` 无法正确统计死亡。
3. **剧情 ID 必须存在**：`storyId` 必须能在 `StoryManager` 的剧情 JSON 中找到，否则播放时会报错。
4. **变量必须先定义**：所有条件中引用的变量必须在 `variables` 列表中定义。
5. **组触发位置**：`triggerPositionX` 必须在 `0 ~ levelLength` 范围内。
6. **背景 Sprite**：单张滚动模式必须指定 `singleBackground`；多层视差模式必须至少有一层。

---

## 8. 示例：创建一个简单关卡

1. 创建 `LevelData`：`Assets/LevelData/MyLevel.asset`。
2. 配置 `levelLength = 60`，`playerSpawnPosition = (2, 0)`，`playerPrefab = Player`。
3. 背景：`SingleInfiniteScroll`，指定 StreetBG Sprite。
4. 添加一个元素组：
   - `groupId = "g1"`
   - `triggerPositionX = 8`
   - `mustClearToProceed = true`
5. 添加两个敌人元素，设置 `groupId = "g1"`，位置分别为 `(12, 0)` 和 `(14, 0)`。
6. 添加剧情触发器：
   - `positionX = 5`
   - `storyId = "level1_opening"`
7. 设置 `levelEndPositionX = 50`。
8. 点击 **Validate** → **Play Test**。

---

## 9. 常见问题

**Q：敌人没有生成？**
A：检查元素组 `triggerPositionX` 是否小于玩家当前位置，以及 `triggerConditions` 是否满足。

**Q：剧情没有播放？**
A：检查 `storyId` 是否拼写正确，并且已加载到 `StoryManager` 中。

**Q：玩家可以走出屏幕？**
A：确保 `LevelCameraController` 已附加到主相机，并且当前存在 `mustClearToProceed` 的激活组。

**Q：导出的场景缺少某些对象？**
A：导出前请在 Play Test 中确认所有组、剧情、独立元素都能正常生成。
