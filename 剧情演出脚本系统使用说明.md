# 剧情演出脚本系统使用说明

## 一、系统分工

新的工作流将“什么时候播放、哪句进入演出、具体怎么演、由谁来演”分别交给对应编辑器：

| 内容 | 编辑位置 | 负责的问题 |
| --- | --- | --- |
| 条件触发、播放哪段剧情 | 关卡编辑器 | 什么时候播放 |
| 台词、哪句台词进入演出 | 剧情编辑器 | 哪一步开始演出 |
| 移动、动画、朝向、镜头 | 演出编辑器 | 具体怎么演 |
| 演出槽位对应本关角色 | 关卡编辑器 | 本关由谁来演 |

推荐操作顺序：

```text
剧情编辑器：写台词并给目标台词添加演出 Cue
        ↓
演出编辑器：制作具体动作
        ↓
关卡编辑器：配置剧情触发，并绑定本关演员
```

## 二、快速开始

下面以“某句台词出现时，主角向右移动 2 米”为例。

### 第一步：在剧情编辑器标记演出入口

1. 打开 Unity 菜单 `Tools > 剧情编辑器`。
2. 打开剧情 JSON。
3. 选择目标剧情段落。
4. 找到需要开始演出的台词。
5. 在该台词下方点击“添加演出 Cue”。
6. 点击 Cue 中的“新建”，创建一个 `PerformanceScript`。
7. 设置：
   - 触发延迟：`0`。
   - 触发时机：台词开始显示时，或玩家点击进入下一句后。
   - 演出完成前禁止下一句：根据需要开启。
8. 点击“编辑”进入独立演出编辑器。

Cue 跟随当前台词保存，不需要再在关卡编辑器里选择台词 ID。

### 第二步：在演出编辑器制作动作

在演出编辑器中：

1. 将脚本 ID 设置为项目内唯一的 ID，例如 `intro_hero_walk_right`。
2. 添加角色槽位：
   - 槽位 ID：`hero`
   - 显示名称：`主角`
   - 默认待机动画：`idle`
3. 添加动作片段：
   - 动作类型：`MoveActor`
   - 角色槽位：`hero`
   - 开始时间：`0`
   - 持续时间：`1`
   - 坐标模式：`RelativeToStart`
   - 目标/相对位移：`(2, 0)`
   - 自动面向移动方向：开启

完成后点击演出编辑器顶部或底部的“保存”按钮。`Ctrl+S` 也可以保存当前演出资源。

### 第三步：在关卡编辑器绑定演员

1. 打开 Unity 菜单 `Tools > 关卡编辑器`。
2. 选择使用该剧情的关卡。
3. 找到对应流程事件的“播放过场”步骤。
4. 选择刚才编辑的剧情 ID。
5. “剧情演出”区域会显示剧情中配置的 Cue 摘要。
6. 在“本关演员绑定”中，将：
   - `主角 [intro_hero_walk_right]` 绑定为“主角”。
7. 点击关卡编辑器顶部“校验”。
8. 进入 Play Mode 测试。

台词开始显示时，主角会在游戏暂停状态下用 1 秒向右移动 2 个世界单位。

## 三、剧情编辑器

剧情编辑器负责：

- 剧情段落和台词。
- 稳定台词 ID。
- 哪句台词触发演出。
- 演出延迟。
- 演出是否阻塞下一句。
- 引用哪个演出脚本。

剧情编辑器不负责：

- 具体动作参数。
- 本关角色绑定。
- 关卡触发条件。

### 给台词添加演出 Cue

每条台词卡片底部都有“演出 Cue”区域。

点击“添加演出 Cue”后可以：

- 选择已有演出脚本。
- 新建演出脚本。
- 打开独立演出编辑器。
- 设置触发延迟。
- 设置触发时机。
- 设置是否阻塞下一句。
- 删除 Cue。

同一句台词允许添加多个 Cue，这些演出会同时开始。

### 触发延迟

“触发延迟（未缩放秒）”表示台词开始显示后等待多长时间启动演出。

剧情期间 `Time.timeScale = 0`，但该延迟使用未缩放时间，因此仍会正常计时。

### 阻塞下一句

- 关闭：演出与打字机、玩家阅读并行，玩家可以正常推进。
- 开启：文字正常显示，但演出完成前不能进入下一句。

建议：

- 背景动作、轻微走位：关闭。
- 必须完成后才能理解下一句的移动、转身或镜头动作：开启。

### 触发时机

- `台词开始显示时`：该句台词出现时立即按延迟启动演出。
- `玩家点击进入下一句后`：玩家读完并点击继续后才启动演出。

选择“玩家点击进入下一句后”并开启“演出完成前禁止下一句”时，当前点击会先启动演出，演出完成后才进入后续台词。

### 背景遮罩

每段剧情的“段落信息”中可以关闭“对游戏背景应用遮罩/模糊”。

关闭后仍显示剧情对话 UI 和头像，但不会启用背景截图模糊或背景 Volume 权重。

### 稳定台词 ID

Cue 使用台词的稳定 ID。

- 调整台词顺序不会改变 ID。
- 新台词使用当前最大 ID + 1。
- 删除台词时，该台词下的 Cue 一并删除。
- 保存剧情时会检查无效或重复台词 ID。

## 四、演出编辑器

打开方式：

- 在剧情台词 Cue 中点击“编辑”。
- Unity 菜单 `Tools > 演出编辑器`。
- 仍然可以在 Project 中选择 `PerformanceScript` 查看 Inspector，但推荐使用独立演出编辑器。

演出编辑器提供显式“保存”按钮、未保存状态提示和 `Ctrl+S` 快捷键。Unity 会自动标记 ScriptableObject 修改，但只有点击保存或执行 Unity 的工程保存后才明确写入磁盘。

演出脚本只描述：

- 需要哪些角色槽位。
- 每个角色执行什么动作。
- 镜头怎么移动。
- 动作的时间关系。

它不直接引用某个关卡中的角色。

### 脚本 ID

每个 `PerformanceScript` 必须拥有项目内唯一的 `scriptId`。

推荐格式：

```text
chapter01_intro_walk
hero_turn_and_leave
boss_before_battle
```

剧情 JSON 保存的是 `scriptId`，因此脚本被剧情使用后不要随意修改 ID。

可以移动或重命名 `.asset` 文件，只要 `scriptId` 不变。

### 角色槽位

槽位表示演出需要的角色，而不是本关的具体对象。

例如：

| 槽位 ID | 显示名称 | 默认待机动画 |
| --- | --- | --- |
| `hero` | 主角 | `idle` |
| `partner` | 同伴 | `idle` |

同一槽位在不同关卡可以绑定到不同角色。

每个演出脚本的槽位独立。即使两个脚本都使用 `hero`，关卡编辑器也会按“脚本 ID + 槽位 ID”分别保存绑定。

## 五、动作片段

片段开始时间相对于当前演出 Cue 的启动时间。

时间重叠的片段会并行执行。

### MoveActor

控制角色槽位移动。

- `开始时间`：片段启动时间。
- `持续时间`：移动所用时间。
- `角色槽位`：目标角色。
- `World`：目标值为世界坐标。
- `RelativeToStart`：目标值为相对 Cue 启动位置的位移。
- `Linear`：匀速。
- `SmoothStep`：平滑起步和停止。
- `自动面向移动方向`：按横向移动方向自动转身。

示例：

```text
坐标模式：RelativeToStart
目标/相对位移：(3, 0)
持续时间：1.5
```

表示角色相对 Cue 启动位置向右移动 3 个世界单位。

注意：`World` 表示“最终世界坐标”，不是移动量。例如角色起点是 `(3,-3)`，目标世界坐标填写 `(5,2)`，实际位移就是 `X+2、Y+5`。新建片段默认使用 `RelativeToStart`，编辑器也会分别显示“目标世界坐标”或“相对起点位移”。

移动片段默认开启“移动时自动播放动画”：

- 默认动画来自角色槽位的“默认移动动画”。
- 可在片段中填写“移动动画覆盖”。
- 也可在关卡演员绑定中为具体角色填写“移动动画覆盖”。
- 默认在移动完成后恢复待机动画。
- 如果同一轨道已被后续动画覆盖，移动结束不会强制抢回待机动画。

### PlayAnimation

播放 Spine 动画或 Animator 状态。

- 动画名称区分大小写。
- Spine 使用 Track 索引。
- Animator 使用 Layer 索引。
- 混合时间控制切换过渡。
- “剧情结束保留动画”开启时保持最终动画。
- 未开启时恢复槽位配置的待机动画。

`PlayAnimation` 是即时动作，通常将持续时间设置为 `0`。

如果填写持续时间，该时间会延长整个演出脚本的完成时间，可用于阻塞下一句。

### SetFacing

立即设置角色朝向：

- 开启“朝向右边”：向右。
- 关闭：向左。

如果已经使用 `SetFacing`，又不希望后续移动修改朝向，请关闭 `MoveActor` 的“自动面向移动方向”。

### MoveCamera

控制关卡镜头移动。

- `World`：移动到世界坐标。
- `RelativeToStart`：相对 Cue 启动时的镜头位置移动。
- 支持持续时间和缓动方式。

“剧情结束恢复镜头跟随”开启时，剧情结束后镜头重新跟随主角。

关闭时镜头保留在最终位置，之后需要通过关卡流程的“恢复镜头跟随”步骤主动释放。

## 六、关卡编辑器

关卡编辑器负责：

- 剧情的触发条件。
- 播放哪个剧情 ID。
- 本关演员绑定。
- 关卡元素和 `elementId`。

关卡编辑器不再负责创建或删除台词 Cue。

### 剧情演出摘要

选择“播放过场”步骤后，关卡编辑器显示：

- 剧情中共有多少个 Cue。
- 每个 Cue 对应的台词摘要。
- Cue 引用的演出脚本 ID。
- “编辑剧情演出”按钮。
- “编辑演出”按钮。

“编辑剧情演出”会打开剧情编辑器并定位到当前剧情。

“编辑演出”会打开独立演出编辑器。

### 本关演员绑定

关卡编辑器会汇总当前剧情引用的所有演出脚本槽位。

绑定项按以下组合区分：

```text
演出脚本 ID + 槽位 ID
```

每个槽位可以绑定：

- 主角。
- 当前关卡拥有有效 `elementId` 的元素。

例如：

| 演出槽位 | 本关绑定 |
| --- | --- |
| 主角 `[intro_walk/hero]` | 主角 |
| 村长 `[intro_talk/elder]` | 村长 `npc_elder` |

还可以为每个绑定填写“待机覆盖”，覆盖演出脚本槽位的默认待机动画。

### 普通关卡角色要求

绑定普通元素时必须保证：

- 元素拥有非空 `elementId`。
- 同一关卡内 `elementId` 唯一。
- 元素在演出触发前已经生成。

目标尚未生成、已经销毁或解析失败时，运行时记录警告并跳过相关动作，不会阻塞剧情。

## 七、运行时规则

### 暂停状态

剧情期间继续保持：

```csharp
Time.timeScale = 0f;
```

演出系统使用未缩放时间：

- 角色演出移动正常执行。
- 镜头正常移动。
- Spine 手动按未缩放时间更新。
- Animator 临时使用 UnscaledTime。
- 普通敌人 AI、战斗和常规物理保持冻结。

### 并行和覆盖

- 不同角色和不同通道可以并行。
- 同一角色的新移动覆盖旧移动。
- 同一动画轨道的新动画覆盖旧动画。
- 新镜头移动覆盖旧镜头移动。

### 正常结束

演出结束时：

- 角色停在最终位置。
- Transform、Rigidbody2D 和 PlayerMovement 内部基准同步。
- 默认恢复待机动画。
- 标记“保留动画”的片段保持最终动画。
- 根据脚本设置恢复或保留镜头。
- 清理临时未缩放动画状态。
- 最后恢复游戏时间和玩家控制。

## 八、跳过剧情

跳过顺序：

1. 禁止重复跳过和剧情推进。
2. 黑幕用未缩放时间淡入。
3. 应用剧情全部 Cue 的最终状态。
4. 所有角色和镜头落到最终位置。
5. 按保留动画或恢复待机规则收尾。
6. 结束剧情并恢复游戏状态。
7. 黑幕淡出。

黑幕完全覆盖前不会恢复玩家输入，避免看到角色瞬移。

## 九、完整示例

目标：

- 某句台词出现后主角转向右边。
- 0.2 秒后播放走路动画。
- 主角向右移动 2 米。
- 镜头同时向右移动 1 米。
- 演出完成前不能进入下一句。

### 剧情编辑器

在目标台词下添加 Cue：

```text
演出脚本：intro_hero_approach
触发延迟：0
演出完成前禁止下一句：开启
```

### 演出编辑器

```text
脚本 ID：intro_hero_approach
剧情结束恢复镜头跟随：开启

槽位：
  hero / 主角 / idle
```

片段：

```text
0.0 秒：SetFacing(hero, 右)
0.2 秒：PlayAnimation(hero, walk, 循环)
0.2 秒：MoveActor(hero, RelativeToStart (2, 0), 1.2 秒)
0.2 秒：MoveCamera(RelativeToStart (1, 0), 1.2 秒)
```

### 关卡编辑器

在 PlayStory 步骤中：

```text
剧情 ID：目标剧情
本关演员绑定：
  主角 [intro_hero_approach] → 主角
```

## 十、校验和常见问题

### 剧情无法保存

检查：

- 台词 ID 是否为正数且不重复。
- Cue 是否仍指向有效台词。
- Cue 是否选择了演出脚本。
- 演出脚本 ID 是否存在或重复。

### 关卡提示没有绑定演员

检查：

- 是否在剧情编辑器保存了最新 JSON。
- PlayStory 是否选择了正确剧情。
- 是否在“本关演员绑定”中为每个脚本槽位选择角色。

### 找不到演出脚本

关卡和剧情通过 `scriptId` 关联演出资源。

检查：

- 演出脚本是否仍在项目 `Assets` 中。
- `scriptId` 是否被修改。
- 是否存在两个相同 `scriptId` 的资源。

### 动画不播放

检查：

- 动画名大小写是否正确。
- Spine SkeletonDataAsset 中是否存在动画。
- Animator 中是否存在对应 State。
- Track 或 Layer 是否正确。
- 是否被同轨道的后续动画覆盖。

### 镜头演出后立刻回到主角

这是“剧情结束恢复镜头跟随”开启时的正常行为。

需要保留镜头位置时关闭该选项，并在后续关卡流程中添加“恢复镜头跟随”。

### 阻塞时间异常

演出完成时间是所有片段中最大的：

```text
开始时间 + 持续时间
```

检查即时动画片段是否误填了较长持续时间。

## 十一、JSON 数据归属

剧情 JSON 保存：

- 台词。
- 台词稳定 ID。
- 演出 Cue。
- `scriptId`。
- 延迟和阻塞设置。

关卡 JSON 保存：

- PlayStory 剧情 ID。
- 本关演员绑定。
- 演出脚本资源 ID 缓存。

演出 `.asset` 保存：

- 角色槽位。
- 动作片段。
- 时间、坐标和动画参数。

因此：

- 修改台词和 Cue 后保存剧情 JSON。
- 修改动作后保存演出资源。
- 修改本关演员后保存关卡资源。

## 十二、旧数据兼容

旧版将台词 Cue 直接保存在关卡 PlayStory 步骤中。

当前运行时仍保留兼容：

- 剧情中没有新 Cue 时，继续读取旧关卡 Cue。
- 剧情中存在新 Cue 时，优先使用剧情编辑器中的新配置。

新内容应统一使用新的三段式工作流，不再在关卡中创建台词 Cue。

## 十三、编辑窗口切换

Unity 的 `EditorWindow` 不支持像操作系统普通窗口一样单独最小化，这是 Unity 编辑器本身的限制。推荐将三个窗口拖进 Unity 主窗口：

- 停靠为三个标签页。
- 或并排停靠剧情与演出，把关卡编辑器放在另一组标签中。

窗口停靠后，Unity 会保存当前布局，下次打开项目时通常可以恢复。
